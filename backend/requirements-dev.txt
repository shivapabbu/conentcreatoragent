# Optional development dependencies
# Install with: pip install -r requirements-dev.txt

# Vector database for local development (optional)
# If installation fails, the app will work with mock responses
chromadb>=0.4.0

